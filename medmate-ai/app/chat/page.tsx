import { ChatWindow } from "@/components/chat/chat-window";

export const metadata = {
  title: "AI Chat",
  description:
    "Start a free, private health consultation with OzDoc — Australia's AI doctor assistant.",
};

export default function ChatPage() {
  return <ChatWindow />;
}
